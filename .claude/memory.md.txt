# Claude Memory — Apartment Management

## Những gì đã làm
- Fix @PathVariable / @RequestParam thiếu explicit name ở tất cả controllers
  (Spring cần `-parameters` compiler flag hoặc explicit name)

## Gotchas đã gặp
- Spring không resolve parameter names từ bytecode nếu thiếu `-parameters` flag
  → luôn dùng `@PathVariable("id")`, `@RequestParam("name")` explicit

## TODO / Backlog
<!-- Cập nhật khi có task mới -->